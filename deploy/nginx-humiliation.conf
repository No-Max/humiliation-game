# Игра
server {
  listen 80;
  listen [::]:80;
  server_name ingames.by www.ingames.by ingame.by www.ingame.by;

  root /opt/humiliation-game/apps/web/dist;
  index index.html;
  add_header X-Robots-Tag "noindex, nofollow, noarchive" always;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    client_max_body_size 5m;
    proxy_pass http://127.0.0.1:3200;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }

  location /uploads/ {
    proxy_pass http://127.0.0.1:3200;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }

  location /socket.io/ {
    proxy_pass http://127.0.0.1:3200;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }
}

# Админка
server {
  listen 80;
  listen [::]:80;
  server_name admin.ingames.by admin.ingame.by;

  root /opt/humiliation-game/apps/admin/dist;
  index index.html;
  add_header X-Robots-Tag "noindex, nofollow, noarchive" always;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    client_max_body_size 5m;
    proxy_pass http://127.0.0.1:3200;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }

  location /uploads/ {
    proxy_pass http://127.0.0.1:3200;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }
}

# Fallback по IP (пока DNS не настроен)
server {
  listen 80 default_server;
  listen [::]:80 default_server;
  server_name _;

  root /opt/humiliation-game/apps/web/dist;
  index index.html;
  add_header X-Robots-Tag "noindex, nofollow, noarchive" always;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    client_max_body_size 5m;
    proxy_pass http://127.0.0.1:3200;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }

  location /uploads/ {
    proxy_pass http://127.0.0.1:3200;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }

  location /socket.io/ {
    proxy_pass http://127.0.0.1:3200;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }
}

# Админка по IP :8080 (legacy, можно убрать после перехода на admin.ingames.by)
server {
  listen 8080;
  listen [::]:8080;
  server_name _;

  root /opt/humiliation-game/apps/admin/dist;
  index index.html;
  add_header X-Robots-Tag "noindex, nofollow, noarchive" always;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    client_max_body_size 5m;
    proxy_pass http://127.0.0.1:3200;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }

  location /uploads/ {
    proxy_pass http://127.0.0.1:3200;
    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
  }
}
